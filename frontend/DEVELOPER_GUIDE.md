# دليل المطور - Frontend (React.js)

## 📋 نظرة عامة على المشروع

هذا هو الجزء الأمامي (Frontend) من مشروع صالون الجمال، مبني باستخدام:
- **React.js 18** مع Vite
- **Tailwind CSS** للتصميم
- **React Router** للتنقل
- **Context API** لإدارة الحالة
- **Axios** للاتصال بالـ API

## 🏗️ هيكل المشروع

```
frontend/
├── public/                     # الملفات الثابتة
│   ├── images/                # الصور
│   ├── fonts/                 # الخطوط
│   └── favicon.ico            # أيقونة الموقع
├── src/
│   ├── components/            # مكونات React
│   │   ├── ui/               # مكونات واجهة المستخدم
│   │   ├── booking/          # مكونات الحجز
│   │   ├── blog/             # مكونات المدونة
│   │   └── payment/           # مكونات الدفع
│   ├── pages/                # صفحات التطبيق
│   ├── services/             # خدمات API
│   ├── contexts/             # Context Providers
│   ├── hooks/                # Custom Hooks
│   ├── utils/                # أدوات مساعدة
│   ├── styles/               # ملفات CSS
│   └── config/              # إعدادات التطبيق
├── package.json              # تبعيات المشروع
├── tailwind.config.ts        # إعدادات Tailwind
├── vite.config.js           # إعدادات Vite
└── README.md                # دليل سريع
```

## 🚀 البدء السريع

### 1. تثبيت التبعيات
```bash
npm install
```

### 2. تشغيل خادم التطوير
```bash
npm run dev
```

### 3. بناء المشروع للإنتاج
```bash
npm run build
```

### 4. معاينة الإنتاج
```bash
npm run preview
```

## 📱 الصفحات الرئيسية

### 1. الصفحة الرئيسية (`HomePage.jsx`)
- **المكونات**: HeroSection, ServicesSection, TestimonialsMarquee
- **الميزات**: عرض الخدمات، العروض، الشهادات
- **التصميم**: متجاوب مع جميع الأجهزة

### 2. صفحة الخدمات (`ServicesPage.jsx`)
- **المكونات**: ServiceSelection, CategoryServices
- **الميزات**: تصفية الخدمات، البحث، التصنيف
- **التفاعل**: اختيار الخدمة للمتابعة للحجز

### 3. صفحة الحجز (`EnhancedBookingPage.jsx`)
- **المكونات**: 
  - `ServiceSelection` - اختيار الخدمة
  - `DateTimeSelection` - اختيار التاريخ والوقت
  - `LocationSelection` - اختيار الموقع
  - `BookingConfirmation` - تأكيد الحجز
- **الميزات**: 
  - تدفق حجز متعدد الخطوات
  - التحقق من صحة البيانات
  - حفظ التقدم تلقائياً
  - تصميم متجاوب

### 4. صفحة العروض (`OffersPage.jsx`)
- **المكونات**: OfferCard, OfferDetailModal
- **الميزات**: عرض العروض، التفاصيل، التطبيق
- **التصميم**: بطاقات تفاعلية مع ألوان مخصصة

### 5. صفحة المدونة (`BlogPage.jsx`)
- **المكونات**: BlogPost, BlogCategory
- **الميزات**: عرض المقالات، التصنيف، البحث
- **التفاعل**: قراءة المقالات، التعليقات

## 🧩 المكونات الرئيسية

### 1. مكونات الحجز (`components/booking/`)

#### `EnhancedBookingPage.jsx`
```jsx
// الصفحة الرئيسية للحجز
- تدفق متعدد الخطوات
- حفظ التقدم تلقائياً
- التحقق من صحة البيانات
- تصميم متجاوب
```

#### `ServiceSelection.jsx`
```jsx
// اختيار الخدمة
- عرض الخدمات المتاحة
- تصفية حسب الفئة
- عرض الأسعار والمدة
- اختيار الموظف
```

#### `DateTimeSelection.jsx`
```jsx
// اختيار التاريخ والوقت
- تقويم تفاعلي
- عرض الأوقات المتاحة
- التحقق من التوفر
- عرض تفاصيل الموظف
```

#### `LocationSelection.jsx`
```jsx
// اختيار الموقع
- خريطة تفاعلية
- إضافة عنوان جديد
- حفظ العناوين
- التحقق من الموقع
```

#### `BookingConfirmation.jsx`
```jsx
// تأكيد الحجز
- مراجعة التفاصيل
- إدخال معلومات إضافية
- تأكيد الحجز
- إرسال الإشعارات
```

### 2. مكونات واجهة المستخدم (`components/ui/`)

#### مكونات أساسية
- **Button** - أزرار مخصصة
- **Card** - بطاقات محتوى
- **Modal** - نوافذ منبثقة
- **Input** - حقول الإدخال
- **Select** - قوائم الاختيار
- **DatePicker** - منتقي التاريخ
- **TimePicker** - منتقي الوقت

#### مكونات متقدمة
- **LoadingSpinner** - مؤشر التحميل
- **ErrorBoundary** - معالج الأخطاء
- **Toast** - إشعارات سريعة
- **ProgressBar** - شريط التقدم
- **Carousel** - عرض شرائح

### 3. مكونات الخدمات

#### `ServicesSection.jsx`
```jsx
// قسم الخدمات في الصفحة الرئيسية
- عرض الخدمات المميزة
- روابط سريعة للحجز
- تصميم جذاب
```

#### `ServiceSinglePage.jsx`
```jsx
// صفحة خدمة واحدة
- تفاصيل الخدمة
- معرض الصور
- معلومات الموظفين
- زر الحجز
```

#### `CategoryServices.jsx`
```jsx
// خدمات الفئة
- تصفية حسب الفئة
- عرض الخدمات
- البحث والترتيب
```

## 🔧 الخدمات والـ API

### 1. خدمات API (`services/`)

#### `api.js` - الخدمة الرئيسية
```javascript
// إعدادات API الأساسية
const API_BASE_URL = 'http://localhost:8000/api';

// وظائف API
- getServices()           // جلب الخدمات
- getCategories()         // جلب الفئات
- getStaff()              // جلب الموظفين
- createBooking()         // إنشاء حجز
- getBookings()           // جلب الحجوزات
- updateBooking()         // تحديث حجز
- deleteBooking()         // حذف حجز
```

#### `apiService.js` - خدمات متقدمة
```javascript
// خدمات متقدمة للـ API
- handleApiErrors()       // معالجة أخطاء API
- retryRequest()          // إعادة المحاولة
- cacheResponse()         // تخزين مؤقت
- validateData()          // التحقق من البيانات
```

#### `maps.js` - خدمات الخرائط
```javascript
// خدمات Google Maps
- initializeMap()         // تهيئة الخريطة
- addMarker()             // إضافة علامة
- geocodeAddress()        // تحويل العنوان لإحداثيات
- reverseGeocode()         // تحويل الإحداثيات لعنوان
```

### 2. إدارة الحالة (`contexts/`)

#### `AuthContext.jsx`
```javascript
// إدارة مصادقة المستخدم
const AuthContext = createContext();

// الوظائف
- login()                 // تسجيل الدخول
- logout()                // تسجيل الخروج
- register()              // تسجيل حساب جديد
- resetPassword()         // إعادة تعيين كلمة المرور
- updateProfile()         // تحديث الملف الشخصي
```

#### `CartContext.jsx`
```javascript
// إدارة سلة التسوق
const CartContext = createContext();

// الوظائف
- addToCart()             // إضافة للسلة
- removeFromCart()        // حذف من السلة
- updateQuantity()        // تحديث الكمية
- clearCart()              // مسح السلة
- getTotal()              // حساب الإجمالي
```

## 🎨 التصميم والأنماط

### 1. Tailwind CSS
```css
/* إعدادات Tailwind مخصصة */
- ألوان مخصصة للعلامة التجارية
- خطوط عربية
- تصميم متجاوب
- أنماط مخصصة للمكونات
```

### 2. ملفات CSS المخصصة

#### `styles/premium-cards.css`
```css
/* أنماط البطاقات المميزة */
- تأثيرات الظلال
- انتقالات سلسة
- ألوان متدرجة
- تأثيرات التفاعل
```

#### `styles/theme-utilities.css`
```css
/* أدوات الثيم */
- متغيرات الألوان
- أنماط الخطوط
- مسافات مخصصة
- تأثيرات الحركة
```

### 3. الألوان والثيم

#### نظام الألوان
```javascript
// ألوان العلامة التجارية
primary: '#e91e63',        // وردي أساسي
secondary: '#9c27b0',     // بنفسجي ثانوي
accent: '#ff9800',        // برتقالي مميز
success: '#4caf50',       // أخضر نجاح
warning: '#ff9800',       // برتقالي تحذير
error: '#f44336',         // أحمر خطأ
```

#### الخطوط
```css
/* خطوط عربية */
@import url('https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;600;700&display=swap');

/* خطوط إنجليزية */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
```

## 🔧 Custom Hooks

### 1. `useApi.js`
```javascript
// Hook لإدارة API calls
const useApi = (url, options) => {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);
  
  // منطق API call
  return { data, loading, error, refetch };
};
```

### 2. `useBookingOptimization.js`
```javascript
// Hook لتحسين عملية الحجز
const useBookingOptimization = () => {
  // تحسين الأداء
  // تخزين مؤقت للبيانات
  // تحسين الشبكة
  // إدارة الحالة
};
```

### 3. `useDynamicColors.js`
```javascript
// Hook للألوان الديناميكية
const useDynamicColors = (category) => {
  // ألوان ديناميكية حسب الفئة
  // تخصيص الثيم
  // إدارة الألوان
};
```

### 4. `use-mobile.js`
```javascript
// Hook للكشف عن الأجهزة المحمولة
const useMobile = () => {
  // كشف الأجهزة المحمولة
  // تحسين الواجهة
  // إدارة التخطيط
};
```

## 🛠️ الأدوات المساعدة (`utils/`)

### 1. `validation.js`
```javascript
// أدوات التحقق من صحة البيانات
- validateEmail()          // التحقق من البريد الإلكتروني
- validatePhone()          // التحقق من رقم الهاتف
- validateRequired()       // التحقق من الحقول المطلوبة
- validateDate()           // التحقق من التاريخ
- validateTime()           // التحقق من الوقت
```

### 2. `apiTest.js`
```javascript
// أدوات اختبار API
- testConnection()         // اختبار الاتصال
- testEndpoints()          // اختبار النقاط
- validateResponse()       // التحقق من الاستجابة
```

### 3. `performance.js`
```javascript
// أدوات تحسين الأداء
- lazyLoadImages()         // تحميل الصور عند الحاجة
- optimizeBundle()         // تحسين الحزمة
- cacheData()              // تخزين البيانات
- debounceFunction()       // تأخير الوظائف
```

### 4. `accessibility.js`
```javascript
// أدوات إمكانية الوصول
- addAriaLabels()          // إضافة تسميات ARIA
- manageFocus()            // إدارة التركيز
- keyboardNavigation()    // التنقل بالكيبورد
- screenReaderSupport()    // دعم قارئ الشاشة
```

## 📱 التصميم المتجاوب

### 1. نقاط التوقف (Breakpoints)
```css
/* نقاط التوقف المخصصة */
sm: '640px',              // أجهزة صغيرة
md: '768px',              // أجهزة متوسطة
lg: '1024px',             // أجهزة كبيرة
xl: '1280px',             // أجهزة كبيرة جداً
2xl: '1536px'             // شاشات عالية الدقة
```

### 2. التخطيط المتجاوب
```jsx
// مثال على التخطيط المتجاوب
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  {/* محتوى متجاوب */}
</div>
```

### 3. الصور المتجاوبة
```jsx
// صور متجاوبة
<img 
  src={imageUrl} 
  alt="وصف الصورة"
  className="w-full h-auto object-cover"
  loading="lazy"
/>
```

## 🔐 الأمان

### 1. حماية البيانات
```javascript
// تشفير البيانات الحساسة
- تشفير كلمات المرور
- حماية البيانات الشخصية
- التحقق من صحة البيانات
- حماية من XSS
```

### 2. إدارة الجلسات
```javascript
// إدارة جلسات المستخدم
- حفظ بيانات الجلسة
- انتهاء صلاحية الجلسة
- تحديث الرموز المميزة
- تسجيل الخروج الآمن
```

## 🚀 النشر والإنتاج

### 1. بناء المشروع
```bash
# بناء للإنتاج
npm run build

# معاينة الإنتاج
npm run preview

# تحليل الحزمة
npm run analyze
```

### 2. تحسين الأداء
```javascript
// تحسينات الأداء
- Code Splitting
- Lazy Loading
- Image Optimization
- Bundle Optimization
- Caching Strategies
```

### 3. متغيرات البيئة
```bash
# متغيرات البيئة
VITE_API_URL=http://localhost:8000/api
VITE_GOOGLE_MAPS_API_KEY=your_api_key
VITE_APP_NAME=صالون الجمال
VITE_APP_VERSION=1.0.0
```

## 🐛 استكشاف الأخطاء

### 1. مشاكل شائعة

#### خطأ في API
```javascript
// معالجة أخطاء API
try {
  const response = await api.get('/services');
} catch (error) {
  console.error('API Error:', error);
  // معالجة الخطأ
}
```

#### مشاكل في التصميم
```css
/* إصلاح مشاكل CSS */
- تحقق من Tailwind classes
- راجع responsive breakpoints
- تأكد من import الملفات
```

#### مشاكل في الأداء
```javascript
// تحسين الأداء
- استخدم React.memo()
- استخدم useMemo() و useCallback()
- تحقق من re-renders
- استخدم lazy loading
```

### 2. أدوات التطوير

#### React Developer Tools
- فحص المكونات
- تتبع الحالة
- تحليل الأداء
- كشف الأخطاء

#### Browser DevTools
- فحص الشبكة
- تحليل الأداء
- كشف الأخطاء
- تحسين التحميل

## 📞 الدعم والمساعدة

### ملفات مهمة للمراجعة
- `src/App.jsx` - المكون الرئيسي
- `src/components/` - جميع المكونات
- `src/services/api.js` - خدمات API
- `src/contexts/` - إدارة الحالة
- `src/utils/` - الأدوات المساعدة

### نصائح للمطور الجديد
1. ابدأ بفهم `App.jsx` والـ routing
2. راجع المكونات في `components/`
3. فهم خدمات API في `services/`
4. تعلم Context API في `contexts/`
5. استخدم Custom Hooks في `hooks/`

## 🔄 التحديثات المستقبلية

### ميزات مخططة
- [ ] PWA (Progressive Web App)
- [ ] Push Notifications
- [ ] Offline Support
- [ ] Dark Mode
- [ ] Multi-language Support
- [ ] Advanced Analytics

### تحسينات الأداء
- [ ] Service Workers
- [ ] Advanced Caching
- [ ] Image Optimization
- [ ] Code Splitting
- [ ] Bundle Analysis

---

**هذا المشروع تم تطويره بعناية مع التركيز على سهولة الصيانة والتطوير. جميع المكونات منظمة ومنسقة لسهولة الفهم والتعديل.** 🚀
